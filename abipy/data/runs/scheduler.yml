# number of weeks to wait.
#weeks:
# number of days to wait.
#days:
# number of hours to wait.
#hours:
# number of minutes to wait.
#minutes:
# number of seconds to wait.
seconds: 5
# when to first execute the job and start the counter (default is after the given interval).
#start_date:
# Send mail to the specified address (accepts string or list of strings).
#mailto: larry.wall@uglyperl.com
#True if the task manager must be re-initialized from 
#file before launching the jobs. Default: False
#use_dynamic_manager: False

# The scheduler will shutdown when the number of python exceptions is > MAX_NUM_PYEXCS
#max_num_pyexcs: 2
# The scheduler will shutdown when the number of Abinit errors is > MAX_NUM_ABIERRS
#max_num_abierrs: 0
# The scheduler will shutdown when the total number of tasks launched is > SAFETY_RATIO * tot_num_tasks.
#safety_ratio: 3
# Send an e-mail to remind the user that the scheduler is running every REMINDME_S seconds
# Default is 2 weeks, the scheduler will shutdown if the mail cannot be sent. 
#remind_me_s: 1209600
#max_time_s:

#max_ncores_used: 2
#customer_service_dir: customer
