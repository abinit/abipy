# TaskManager configuration options.
qtype: shell
mpi_runner: mpirun
pre_run:
    - "source ~/Coding/Abinit/bzr_archives/env.sh"
policy:
    autoparal: 1
    max_ncpus: 2
    #condition:
    #vars_condition: {"npfft": {"$eq": 1}}
